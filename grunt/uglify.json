{
  "default" : {
    "files" : [{
      "expand" : true,
      "cwd" : "builds/latest.safariextension",
      "src" : ["**/*.js"],
      "dest" : "builds/latest.safariextension/<%= srcDir %>"
    }]
  }
}
